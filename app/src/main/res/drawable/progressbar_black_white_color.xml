<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 背景  gradient是渐变,corners定义的是圆角 -->
    <item android:id="@android:id/background">
        <shape>
            <corners android:radius="@dimen/dp_11" />

            <solid android:color="@color/color_e6e6e6" />
        </shape>
    </item>
    <!-- 第二条进度条颜色 -->
    <item android:id="@android:id/secondaryProgress">
        <clip>
            <shape>
                <corners android:radius="@dimen/dp_11" />

                <gradient
                    android:angle="90.0"
                    android:centerColor="@color/color_eb9d4f"
                    android:centerY="0.45"
                    android:endColor="@color/color_eb9d4f"
                    android:startColor="@color/color_eb9d4f" />
            </shape>
        </clip>
    </item>
    <!-- 进度条 -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <corners android:radius="@dimen/dp_11" />
                <solid android:color="@color/color_eb9d4f" />
            </shape>
        </clip>
    </item>

</layer-list>